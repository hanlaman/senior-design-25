# --- Xcode ---
# Ignore build artifacts and derived data
build/
DerivedData/
*.moved-aside
*.xcuserstate

# Ignore user-specific settings (IMPORTANT)
# These cause merge conflicts if committed
xcuserdata/
*.mode1v3
*.mode2v3
*.perspectivev3

# --- Swift Package Manager ---
# Ignore local build artifacts but keep Package.resolved if you want pinned versions
.build/
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata

# --- CocoaPods (if used) ---
# It is best practice to ignore the Pods directory in monorepos to save space
Pods/
!Podfile.lock

# --- Fastlane ---
fastlane/report.xml
fastlane/test_output/
fastlane/preview.html

# --- Other Apple Artifacts ---
*.hmap
*.ipa
*.dSYM.zip
*.dSYM